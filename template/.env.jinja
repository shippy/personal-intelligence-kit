# {{ vault_name }} — Secrets (gitignored)
# Fill in values as needed. See .env.example for reference.

ANTHROPIC_API_KEY=
{% if enable_tasks and task_provider == 'todoist' -%}
TODOIST_API_TOKEN=
{% endif -%}
{% if enable_contacts_mcp -%}
CLAY_API_KEY=
{% endif -%}
